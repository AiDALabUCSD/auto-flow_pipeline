[build-system]
requires = ["setuptools>=63.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "auto-flow_pipeline"
version = "0.1.0"
description = "This is the pipeline to use AutoFlow. Further details pending..."
dependencies = []

# Optional if you want to create a command-line entry point:
# [project.scripts]
# my_vessel_pipeline = "src.pipeline_runner:main"
